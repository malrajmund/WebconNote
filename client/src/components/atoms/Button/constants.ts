import { ButtonVariantType } from './Button.types';

export const ButtonVariant: Record<ButtonVariantType, ButtonVariantType> = {
    light: 'light',
    dark: 'dark',
    icon: 'icon',
    note: 'note',
};
