import { NoteVariantType } from './NoteListItem.types';

export const NoteVariant: Record<NoteVariantType, NoteVariantType> = {
    primary: 'primary',
    secondary: 'secondary',
    tertiary: 'tertiary',
};
